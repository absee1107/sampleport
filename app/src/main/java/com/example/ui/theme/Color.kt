package com.example.ui.theme

import androidx.compose.ui.graphics.Color

val HighDensityPrimary = Color(0xFF8B5A2B) // Warm rich traditional leather brown
val HighDensitySecondary = Color(0xFF704A24) // Earthy brown
val HighDensityTertiary = Color(0xFFC0392B) // Ledger red
val HighDensityBackground = Color(0xFFFFFDF9) // Beautiful vintage cream diary paper background
val HighDensitySurface = Color(0xFFFAEDE0) // Warm paper-wrapped card surfaces
val HighDensityOnBackground = Color(0xFF3E2723) // Deep espresso brown text
val HighDensityOnSurface = Color(0xFF3E2723) // Deep espresso brown text

// Quick Stats (High Density Grid Colors - pastel Indian theme)
val EarningsBg = Color(0xFFE2F4E9) // Pastel green for Kamaai
val EarningsText = Color(0xFF1B5E20) // Deep Green text

val ExpensesBg = Color(0xFFFDECEB) // Pastel red for Kharcha
val ExpensesText = Color(0xFFB71C1C) // Deep Red text

val SavingsBg = Color(0xFFE1F5FE) // Pastel blue for Bachat
val SavingsText = Color(0xFF01579B) // Deep Blue text

val BorderSlate = Color(0xFFEFE6D5) // Vintage border line

// Semantic colors for labels/charts
val FinanceEarning = Color(0xFF2E7D32) // Forest green
val FinanceExpense = Color(0xFFC62828) // Crimson red
val FinanceSaving = Color(0xFF1565C0)  // Royal blue

